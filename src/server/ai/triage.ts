/**
 * AI 分类 / 摘要（M3 实现）。当前为占位实现。
 */
export async function triageMessage(accountId: string, messageId: string): Promise<void> {
  console.log(`[ai] triage 占位：账号 ${accountId} 邮件 ${messageId}（M3 实现）`);
}
